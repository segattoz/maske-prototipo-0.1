[build]
  base = "maske-simplify-shine-main"
  command = "npm run build"
  publish = "dist"   # troque para "build" se seu build sair em build/

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
